[package]
name = "tabctl-shared"
version = "0.6.0-alpha.18"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
url-normalize = "0.1"
